import type { NextConfig } from "next";

const nextConfig: NextConfig = {
   /* config options here */
   images: {
      domains: ["qaufbqytrhwgeteijboz.supabase.co"], // ✅ Add your Supabase domain
   },
};

export default nextConfig;
